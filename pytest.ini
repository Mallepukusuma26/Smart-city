[pytest]
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --ignore=backend/trained_models --ignore=backend/datasets
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore::SQLAlchemyWarning
